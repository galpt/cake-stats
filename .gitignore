# compiled binary in workspace root (anchored so it does not match cmd/cake-stats/)
/cake-stats

# command binary if built via cmd/
/cmd/cake-stats/cake-stats

# windows executables
*.exe

# easyjson generated helpers (if codegen is run)
**/*_easyjson.go
